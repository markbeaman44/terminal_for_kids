{
  "name": "terminal_for_kids",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "scripts": {
    "start": "npx tsc && vite",
    "build": "npx tsc",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "phaser": "^3.85.2",
    "vite": "^5.4.8"
  }
}
